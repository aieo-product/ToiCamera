# macOS
.DS_Store

# Secrets — never commit credentials (akc / wrangler secret で管理)
secrets.ini
.env
.dev.vars

# PlatformIO
.pio/
.vscode/

# Node
node_modules/
dist/
.wrangler/

# dev-workflow (internal agent handover/logs — never publish)
.agent/
.claude/handoff/
firmware/cams3/build-tree/

# Local media / working files (demo videos, drafts)
userInput/
video/
